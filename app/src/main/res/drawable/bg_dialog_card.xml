<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Warna dasar dialog -->
    <solid android:color="#1C2A3A" />

    <!-- Sudut membulat -->
    <corners android:radius="24dp" />

    <!-- Padding dalam -->
    <padding
        android:left="20dp"
        android:top="20dp"
        android:right="20dp"
        android:bottom="20dp" />

    <!-- Garis tepi halus -->
    <stroke
        android:width="1dp"
        android:color="#2E3C4A" />
</shape>
